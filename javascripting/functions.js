'use strict';

let eat = function(food) {
  return food + ' tasted really good.';
};

console.log(eat('bananas'));

// when logging the result, make sure that the call includes '' for string
// elements
